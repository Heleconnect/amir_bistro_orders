name: amir_bistro_orders
description: تطبيق إدارة طلبات مطعم مع Firebase, SQLite, طباعة PDF و Bluetooth

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # ================== 📌 إدارة الحالة ==================
  provider: ^6.1.5
  flutter_riverpod: ^2.5.1

  # ================== 📌 Firebase ==================
  firebase_core: ^2.35.0
  cloud_firestore: ^4.23.0
  firebase_auth: ^5.0.2
  firebase_storage: ^11.3.0
  firebase_messaging: ^14.13.0
  firebase_crashlytics: ^3.3.10

  # ================== 📌 SQLite والتخزين المحلي ==================
  sqflite: ^2.5.0
  path: ^1.9.0
  path_provider: ^2.2.0
  shared_preferences: ^2.5.0

  # ================== 📌 PDF والطباعة ==================
  pdf: ^3.12.0
  printing: ^5.16.0
  pdfium: ^1.8.0

  # ================== 📌 طباعة حرارية Bluetooth ==================
  blue_thermal_printer: ^2.0.0
  esc_pos_printer: ^0.4.0
  usb_esc_printer: ^0.3.0

  # ================== 📌 QR Code ==================
  qr_flutter: ^4.2.0
  mobile_scanner: ^2.1.0
  barcode_widget: ^2.0.0

  # ================== 📌 التواريخ واللغات ==================
  intl: ^0.19.0
  flutter_localizations:
    sdk: flutter

  # ================== 📌 HTTP و Networking ==================
  http: ^1.3.0
  dio: ^5.5.0
  connectivity_plus: ^5.0.0

  # ================== 📌 الأدوات المساعدة ==================
  collection: ^1.18.0
  crypto: ^3.0.0
  equatable: ^2.0.5
  get_it: ^7.6.4
  logger: ^2.0.2

  # ================== 📌 UI و Animations ==================
  flutter_svg: ^2.0.10
  lottie: ^3.1.0
  shimmer: ^3.0.0
  badges: ^3.0.0
  flutter_typeahead: ^4.5.0

  # ================== 📌 الإذونات ==================
  permission_handler: ^11.0.1
  location: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/fonts/
    - assets/translations/
    - assets/sounds/

  fonts:
    - family: Cairo
      fonts:
        - asset: assets/fonts/Cairo-Regular.ttf
        - asset: assets/fonts/Cairo-Bold.ttf
          weight: 700
        - asset: assets/fonts/Cairo-Light.ttf
          weight: 300

# ================== 📌 إعدادات الترجمة ==================
flutter_intl:
  enabled: true
